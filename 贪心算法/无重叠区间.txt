class Solution {
    public int eraseOverlapIntervals(int[][] intervals) {
        //按照结束时间从小到大排序
        Arrays.sort(intervals,(a,b)->a[1]-b[1]);
        int n = intervals.length;
        int count = 0;
        int endDay = Integer.MIN_VALUE;
        for(int[] interval : intervals){
            if(interval[0]>=endDay){
                count++;
                endDay = interval[1];
            }
        }
        return n- count;
    }
}